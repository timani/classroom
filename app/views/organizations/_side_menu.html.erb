<nav class="menu">
  <span class="menu-heading">Classroom settings</span>

  <%= link_to 'Classroom profile', edit_organization_path(current_organization),
              class: "menu-item #{'selected' if current_page?(edit_organization_path(current_organization))}" %>

  <% if student_identifier_enabled? %>
    <%= link_to 'Student identifiers', organization_student_identifier_types_path(current_organization),
                class: "menu-item #{'selected' if current_page?(organization_student_identifier_types_path(current_organization))}" %>
  <% end %>

  <% if team_management_enabled? %>
    <%= link_to 'Team management', settings_teams_organization_path(current_organization),
                class: "menu-item #{'selected' if current_page?(settings_teams_organization_path(current_organization))}" %>
  <% end %>

  <%= link_to 'Invite other administrators',
              settings_invitations_organization_path(current_organization),
              class: "menu-item #{'selected' if current_page?(settings_invitations_organization_path(current_organization))}" %>
</nav>
