<li class="clearfix">
  <%= link_to 'Edit', edit_organization_grouping_path(current_organization, grouping), class: 'btn btn-sm right' %>
  <%= octicon 'organization' %>
  <strong><%= link_to grouping.title, organization_grouping_path(current_organization, grouping) %></strong>
</li>
